# 任务提醒(Unified Task Scheduler)

面向个人使用的循环提醒调度器:任务按 cron 表达式到期后通过邮件或 Webhook 发出提醒。

## Language

**任务(Task)**:
一个循环提醒单元,由 cron 表达式定义触发节奏,到期后经由某个渠道发出。_Avoid_: 作业、定时器、job

**分组(Group)**:
任务的命名容器,用于归类管理。还有任务的分组不允许删除。_Avoid_: 分类、类别

**渠道(Channel)**:
提醒的投递媒介,每个任务只绑一个:邮件或 Webhook。_Avoid_: 通知方式

**启用(Enabled)**:
任务的开关状态。停用的任务不参与调度,直到重新启用。

**执行历史(Execution History)**:
一次实际触发的留痕,结果只有成功或失败两种。_Avoid_: 运行记录、日志

**标签(Tag)**:
任务上的自由标注,仅用于查找和筛选任务,完全不影响调度与触发;标签区分大小写。_Avoid_: 分类、类别

**下次执行时间(Next Run Time)**:
根据 cron 表达式推算出的未来触发时刻的预览,不是已发生的记录。

**日历视图(Upcoming Schedule)**:
所有启用任务未来触发时刻的只读总览,按月历呈现(移动端降级为近期列表);它不是可编辑的日程。_Avoid_: 日程、排期
